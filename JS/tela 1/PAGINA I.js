const botaoProdutos = document.getElementById('inQDesc'); // Captura o primeiro botão

botaoProdutos.addEventListener('click', function() { // Adiciona o evento de clique
    window.location.href = "HTML-TELA2-1.html"; // Define a ação de redirecionamento
});

const botaoContato = document.getElementById('inBDesc'); // Captura o segundo botão

botaoContato.addEventListener('click', function() { // Adiciona o evento de clique
    window.location.href = 'HTML-TELA4-1.html'; // Define a ação de redirecionamento
});